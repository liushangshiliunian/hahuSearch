<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fc.mapper.CollectionMapper">
    <select id="listCollectionByCollectionId" resultType="com.fc.model.Collection">
        select collection_id, collection_name, create_time, update_time, followed_count
        from collection
        <where>
            collection_id in
            <foreach collection="list" item="item" index="index" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>

    <select id="listCreatingCollectionByUserId" resultType="com.fc.model.Collection">
        select collection_id , collection_name , create_time, update_time, followed_count
        from collection
        where user_id= #{userId}
    </select>

    <insert id="insertCollection">
        insert into collection(collection_name, create_time, update_time, user_id)
        values (#{collectionName}, #{createTime}, #{updateTime}, #{userId})
    </insert>
</mapper>